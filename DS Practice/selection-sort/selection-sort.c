#include <stdio.h>
#define s 100
int main()
{
    int n, arr[s], index, i;
    printf("Enter Number of Element: ");
    scanf("%d", &n);
    
    for(i=0;i<n;i++)
    {
        scanf("%d", &arr[i]);
    }

    for(i=0;i<n;i++)
    {
        if(a[i]>a[i+1])
        {
            /* code */
        }
        
    }
}